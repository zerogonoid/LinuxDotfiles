# -*- mode: snippet -*-
# name: begm
# key: <begm
# --

#include <bits/stdc++.h>
using namespace std;

/* Created:  `(format-time-string "%Y-%m-%d %H:%M:%S")` */

#ifdef ONPC
#include "debug.h"
#else
#define deb(...)
#define out(...)
#endif

#define sz(a) ((int)((a).size()))

// mt19937 rnd(239);
mt19937 rnd(chrono::steady_clock::now().time_since_epoch().count());

typedef long long ll;
typedef long double ld;

void solve() {
  $0
}

int32_t main() {
  ios::sync_with_stdio(false); cin.tie(nullptr);
  int tt = 1; $1cin >> tt;
  while (tt--) {
    out(0);
    //static int casecount(0); cout << "Case #" << ++casecount << ": ";
    solve();
  }
  out(1);
}
